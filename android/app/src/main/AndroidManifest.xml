<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartlibrary">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".SmartLibraryApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_book"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.SmartLibrary">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartLibrary">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Auth -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".ResetPasswordActivity" />

        <!-- User -->
        <activity android:name=".BookListActivity" />
        <activity android:name=".BookDetailActivity" />
        <activity android:name=".BorrowActivity" />
        <activity android:name=".MyBorrowsActivity" />
        <activity android:name=".ReturnActivity" />
        <activity android:name=".ProfileActivity" />

        <!-- Admin -->
        <activity android:name=".DashboardActivity" />
        <activity android:name=".BorrowRequestsActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".ManageBookActivity" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".SettingsActivity" />

        <!-- Backports per-app language persistence (autoStoreLocales) to API < 33. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

    </application>
</manifest>
